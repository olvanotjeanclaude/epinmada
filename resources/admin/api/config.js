export const APP_BACKEND_URL = "http://localhost:8000";

export const BASE_URL = `${APP_BACKEND_URL}/api`;

export const END_POINTS = {
    users: "users",
    products: "products",
    members: "members",
}